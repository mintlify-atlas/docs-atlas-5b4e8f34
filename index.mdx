---
title: "Caddy Proxy Manager: Modern Web UI for Caddy Server"
description: "Manage reverse proxies, certificates, WAF, mTLS, geo blocking, and analytics through one unified dashboard — no Caddyfile editing required."
---

Caddy Proxy Manager gives you a powerful web interface for [Caddy Server](https://caddyserver.com/), eliminating manual configuration file editing. From reverse proxies and automatic HTTPS to a built-in Web Application Firewall and traffic analytics, everything is managed through a clean, responsive dashboard with a full REST API.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Deploy Caddy Proxy Manager with Docker Compose and set up your first proxy host in minutes.
  </Card>
  <Card title="Configuration" icon="sliders" href="/configuration">
    Explore all environment variables and Docker Compose profiles available for your deployment.
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/overview">
    Authenticate with Bearer tokens and interact with the full REST API at /api/v1/.
  </Card>
  <Card title="Security" icon="shield-halved" href="/guides/waf">
    WAF protection, geo blocking, mTLS with a built-in CA, and automatic HTTPS — all per host.
  </Card>
</CardGroup>

## Core features

<CardGroup cols={2}>
  <Card title="Reverse Proxy Hosts" icon="network-wired" href="/guides/proxy-hosts">
    Route HTTP/HTTPS traffic to any upstream with load balancing, health checks, and custom headers.
  </Card>
  <Card title="L4 TCP/UDP Proxying" icon="layer-group" href="/guides/l4-proxy-hosts">
    Stream-proxy raw TCP and UDP traffic with TLS SNI matching and proxy protocol support.
  </Card>
  <Card title="Web Application Firewall" icon="shield" href="/guides/waf">
    Block SQLi, XSS, LFI, and RCE attacks with Coraza and the OWASP Core Rule Set.
  </Card>
  <Card title="Geo Blocking" icon="earth-americas" href="/guides/geo-blocking">
    Allow or deny traffic by country, continent, ASN, CIDR, or exact IP per proxy host.
  </Card>
  <Card title="Certificates & mTLS" icon="certificate" href="/guides/certificates">
    Automatic ACME certificates, DNS-01 challenge support, and a built-in CA for client certs.
  </Card>
  <Card title="Forward Auth Portal" icon="lock" href="/guides/forward-auth">
    Protect any proxied app with built-in identity management — no external IdP required.
  </Card>
  <Card title="Traffic Analytics" icon="chart-line" href="/guides/analytics">
    Live traffic charts, country maps, top user agents, and WAF event logs powered by ClickHouse.
  </Card>
  <Card title="Instance Sync" icon="arrows-rotate" href="/guides/instance-sync">
    Push configuration from a master to one or more slave instances on every change.
  </Card>
</CardGroup>

## Get started in three steps

<Steps>
  <Step title="Deploy with Docker Compose">
    Clone the repository, copy `.env.example` to `.env`, fill in your credentials, and run `docker compose up -d`. The dashboard is available at `http://localhost:3000`.
  </Step>
  <Step title="Create your first proxy host">
    Log in as admin, navigate to **Proxy Hosts**, and click **Add Proxy Host**. Enter your domain, upstream address, and enable SSL — Caddy obtains a Let's Encrypt certificate automatically.
  </Step>
  <Step title="Secure and monitor">
    Enable the WAF, add geo blocking rules, and turn on ClickHouse analytics to gain full visibility into traffic and threats.
  </Step>
</Steps>

<Note>
  Caddy Proxy Manager requires Docker and Docker Compose. See the [Quickstart](/quickstart) for the full setup guide.
</Note>
